/**
 * Every word on the site lives under `lib/content/`, lifted from Ren's profile
 * README, her CV and the real project repositories so the site and the source
 * material can never drift apart.
 *
 * Import from `@/lib/content` — the split into modules is an implementation
 * detail of this folder.
 */

export * from "./profile";
export * from "./experience";
export * from "./credentials";
export * from "./personality";
export * from "./projects";
export * from "./stack";
